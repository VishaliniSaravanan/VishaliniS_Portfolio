# Vishalini Saravanan - Portfolio

Personal portfolio website built with:

- **React 19** — UI components
- **Vite 8** — build tool & dev server
- **Framer Motion** — animations
- **GSAP** — accordion gallery
- **Lucide React** — icons
- **Vanilla CSS** — all styling

```bash
npm install
npm run dev
```
